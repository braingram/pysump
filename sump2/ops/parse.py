#!/usr/bin/env python
"""
settings = {
    'address': (0, 16),
    'data': (16, 8),
    'litfin': 24,
    'litfout': 25,
    'sysr': 26,
    'systb': 27,
    'dbin': 28,
    'wr': 29,
    'memr': 30,
    'o2': 31,
}

if settings[key] is a tuple = (start bit, length)
if settings[key] is a single value = bit index
"""


def parse(capture, settings):
    """
    """
    pass
